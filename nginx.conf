server {
    listen 80;
    server_name _;

    # Let's Encrypt certificate verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    client_max_body_size 16M;

    location / {
        proxy_pass http://app:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }
}

# Uncomment the block below after obtaining SSL certificate with:
#   sudo certbot certonly --webroot -w /var/lib/docker/volumes/policy-irr-analyzer_certbot-webroot/_data -d YOUR_DOMAIN
# Then replace YOUR_DOMAIN with your actual domain name.

# server {
#     listen 443 ssl;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#
#     client_max_body_size 16M;
#     gzip on;
#     gzip_types text/html text/css application/javascript;
#
#     add_header X-Frame-Options SAMEORIGIN;
#     add_header X-Content-Type-Options nosniff;
#
#     location / {
#         proxy_pass http://app:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#         proxy_send_timeout 120s;
#     }
# }
